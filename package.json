{
  "name": "lanecraft",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:ci": "tsc && vite build",
    "cap:build": "cross-env CAP_BUILD=1 npm run build",
    "cap:sync": "npx cap sync",
    "cap:ios": "npm run cap:build && npx cap sync ios",
    "cap:android": "npm run cap:build && npx cap sync android && npx cap open android",
    "preview": "vite preview",
    "test:sim": "tsc -p tsconfig.simtest.json && node -e \"require('fs').mkdirSync('dist-simtest',{recursive:true});require('fs').writeFileSync('dist-simtest/package.json','{\\\"type\\\":\\\"commonjs\\\"}')\" && node dist-simtest/tests/simSmoke.js",
    "balance": "tsc -p tsconfig.simtest.json && node -e \"require('fs').mkdirSync('dist-simtest',{recursive:true});require('fs').writeFileSync('dist-simtest/package.json','{\\\"type\\\":\\\"commonjs\\\"}')\" && node dist-simtest/tests/balanceSim.js",
    "sanity": "tsc -p tsconfig.simtest.json && node -e \"require('fs').mkdirSync('dist-simtest',{recursive:true});require('fs').writeFileSync('dist-simtest/package.json','{\\\"type\\\":\\\"commonjs\\\"}')\" && node dist-simtest/tests/balanceSim.js 1",
    "cost-analysis": "tsc -p tsconfig.simtest.json && node -e \"require('fs').mkdirSync('dist-simtest',{recursive:true});require('fs').writeFileSync('dist-simtest/package.json','{\\\"type\\\":\\\"commonjs\\\"}')\" && node dist-simtest/tests/costAnalysis.js"
  },
  "devDependencies": {
    "@capacitor/cli": "~8.0.1",
    "@types/node": "^25.3.5",
    "cross-env": "^10.1.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@capacitor/android": "~8.0.1",
    "@capacitor/app": "^8.0.1",
    "@capacitor/core": "~8.0.1",
    "@capacitor/ios": "~8.0.1",
    "firebase": "^12.10.0"
  }
}
